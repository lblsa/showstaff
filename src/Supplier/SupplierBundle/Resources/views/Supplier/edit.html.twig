{% extends 'SupplierBundle::base.html.twig' %}

{% block body %}
	<div class="hero-unit">
		<h2>Supplier {{ supplier.name }}</h2>
		<form action="{{ path('supplier_edit', {'id': supplier.id}) }}" method="post" {{ form_enctype(form) }}>
			{{ form_widget(form) }}
			<input type="submit" value="Save" />
		</form>
	</div>

{% endblock %}
