{% extends 'SupplierBundle::base.html.twig' %}

{% block body %}
	<div class="hero-unit">
		<h2>Add Product</h2>
		<form action="{{ path('supplier_products_edit', {'id': id}) }}" method="post" {{ form_enctype(form) }}>
			{{ form_widget(form) }}

			<input type="submit" value="Save" />
		</form>
	</div>

{% endblock %}
